import React from "react";
import Navbar from './'
const HeroSection = () => {
  return (
    <div className="relative h-screen w-full">
      <Navbar/>
    </div>
  );
};

export default HeroSection;
